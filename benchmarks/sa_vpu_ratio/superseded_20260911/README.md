# Superseded results — do not use for conclusions

The original tile timing oracle reset its clock after initialization, which could add artificial SA completion stalls. The parent directory contains corrected source and rerun results. This archive preserves the original five source files and all original result artifacts.
